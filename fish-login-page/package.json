{
    "name": "fish-login-page",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "npx serve public -l 3000",
        "build": "echo 'Static site, no build needed'"
    },
    "devDependencies": {
        "serve": "^14.0.0"
    }
}